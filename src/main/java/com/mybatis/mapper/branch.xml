<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="Branch">
<!-- 	<insert id="article-insert" parameterType="articledto">
		INSERT INTO ARTICLE a VALUES (article_seq.nextval, #{title}, #{job}, #{career}, #{salary}, #{location}, #{need_amt}, #{deadline}, #{job_explain}, #{qualify},#{tag_stack})
	</insert>
	<insert id="company-insert" parameterType="companydto">
		INSERT INTO COMPANY c VALUES (company_seq.nextval, #{company_no}, #{password}, #{name}, #{birth}, #{type}, #{employee_no}, #{sale}, #{ceo},
							#{url}, #{addr}, ${detail_addr})
	</insert>
	<select id="company-select" parameterType="_int" resultType="companydto">
		SELECT * FROM COMPANY c WHERE ID = #{id}
	</select>
	<select id="article-select" parameterType="_int" resultType="articledto">
		SELECT * FROM ARTICLE a WHERE ID = #{id}
	</select>
	 -->
	<select id="article-idmax" resultType="_int">
		SELECT MAX(ID) FROM ARTICLE a ORDER BY ID DESC
	</select>
	
	<insert id="article-insert" parameterType="articledto">
		INSERT INTO "article" a VALUES (article_seq.nextval, #{title}, (SELECT c."id" FROM COMPANY c WHERE c."id" = #{company_id}), #{career}, #{salary}, (SELECT a.ID FROM ADDRESS a WHERE a.DISTRICT = #{address}), #{detail_addr}, #{need_amt}, #{job_explain}, #{deadline}, #{qualify})
	</insert>
	<insert id="company-insert" parameterType="comdto">
		INSERT INTO COMPANY c VALUES (company_seq.nextval, #{company_no}, #{password}, #{name}, #{date}, (SELECT cs."id"  FROM "c_scale" cs  WHERE cs."title" = #{title}), #{employee_no}, #{ceo}, #{url}, (SELECT a.ID FROM ADDRESS a WHERE a.DISTRICT = #{address}), #{detail_addr}, #{content})
	</insert>
	<insert id="job_insert" parameterType="String">
		INSERT INTO JOB j VALUES (
	   JOB_SEQ.nextval
	   ,   (SELECT max(a.ID) FROM ARTICLE a)
	   ,   #{j_name}
	)
	;
	</insert>
	
	<insert id="stack_insert" parameterType="String">
				INSERT INTO "skill_stack" ss VALUES (
		   SKILL_SEQ.nextval
		   ,   (SELECT max(a."id") FROM "article" a)
		   ,   #{s_name}
		)
		;	
	</insert>
</mapper>







