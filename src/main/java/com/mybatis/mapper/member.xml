<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Member">

	<insert id="memberJoin" parameterType="hashmap">
		INSERT INTO MEMBER(
			ID, USERID , PASSWORD , NAME , GENDER , BIRTHDAY , PHONE , EMAIL , ADDRESS , DETAIL_ADDR)
		VALUES (
				member_seq.NEXTVAL
			,	#{USERID}
			,	#{PASSWORD}
			,	#{NAME}
			,	#{GENDER}
			,	#{BIRTHDAY}
			,	#{PHONE}
			,	#{EMAIL}
			,	#{ADDRESS}
			,	#{DETAIL_ADDR})
	</insert>

</mapper>